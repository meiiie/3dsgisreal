# @loi-vao/shared

Shared TypeScript contracts for app surfaces and workers.

Expected contents:

- scene manifest types.
- public API DTOs.
- place/category/status enums.
- validation schemas once real APIs are added.

Keep this package small. Add code only when at least two apps/packages need the same contract.
