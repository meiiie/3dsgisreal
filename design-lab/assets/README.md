# Selected Design Assets

Use this folder for selected generated raster assets before they become app assets.

Rules:

- keep prompts and selection notes in `design-lab/prompts/`
- do not reference this folder directly from production code
- when an asset is used by the app, promote a final optimized copy into the appropriate app public asset folder
- avoid storing private room scans or heavy 3DGS artifacts here
